#!/bin/bash
set -eu


DIR="$(dirname "$0")"

function usage() {
    echo -n \
"Usage: $(basename "$0")

Starts servers using docker-compose
"
}

if [ "${BASH_SOURCE[0]}" = "${0}" ]
then
    if [ "${1:-}" = "--help" ]
    then
        usage
    else
        TILE_SERVER_LOCATION="http://localhost:8081" \
                            docker-compose \
                            -f docker-compose.java.yml \
                            up nginx-backsplash backsplash api-server nginx-api
    fi
    exit
fi
