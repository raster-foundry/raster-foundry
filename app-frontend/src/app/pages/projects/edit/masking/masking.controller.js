export default class ProjectsMaskingController {
    constructor($log, $state) {
        // eslint-disable-line max-params
        'ngInject';
        this.$log = $log;
        this.$state = $state;
    }
}
