export default class AnnotateController {
    constructor( // eslint-disable-line max-params
        $log, $state, $scope
    ) {
        'ngInject';
        this.$log = $log;
        this.$state = $state;
        this.$scope = $scope;
    }
}
